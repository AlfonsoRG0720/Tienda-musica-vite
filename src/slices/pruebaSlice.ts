/*
import { createSlice } from '@reduxjs/toolkit';

const prueba=false;

const chat = createSlice({
    name: 'chat',
    initialState: prueba,
    reducers: {
        online: (state) => { return true },
        offline: (state) => { return false}
    }

});

export const { online, offline } = chat.actions;

const chatReducer=chat.reducer;
export {chatReducer};



const chatAdmin = createSlice({
    name: 'chat',
    initialState: prueba,
    reducers: {
        online1: (state) => { return true },
        offline1: (state) => { return false}
    }

});

export const { online1, offline1 } = chatAdmin.actions;

const chatReducerAdmin=chatAdmin.reducer;
export {chatReducerAdmin};

*/